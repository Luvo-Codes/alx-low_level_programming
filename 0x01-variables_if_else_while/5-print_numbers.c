#include <stdio.h>
/**
 * main - Entry point
 *
 * Description: prints base10 single digits
 *
 * Return: always 0 (success)
 */
int main(void)
{
	for (int i = 0; i < 10; i++)
		printf(i);
	return (0);
}
